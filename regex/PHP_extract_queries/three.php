
function get_deb_url()
{
	return file_get_contents("../__...txt");
}

function get_deb_session()
{
	session_start();
	if(!...) die("INVALID SESSION @GET_DEB_SESSION");
	return $....['..'];
}

function get_meta_logs($user_aid)
{
	$user	= (int)$xxx;
	$query	= "SELECT access.xxxx($xxx);";
	$result	= pg_query(xx(), $query);
	$out	= pg_fetch_row($result)[0];	
	return ($x) ? $x : -x;
}
